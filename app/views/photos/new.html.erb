<h1>Add a photo</h1>

<%= form_for current_user, :url => users_path, :html => { :multipart => true } do |form| %>
  <%= form.file_field :file %>
  <%= submit_tag "Upload", photos_path, :class => "btn btn-primary" %>
<% end %>
